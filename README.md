# IP Address Tracker

Shows the location of the user's IP address on a map with descriptions.

## How it works
Uses an IP geolocation API to fetch location data and displays it on an interactive map.

Open `index.html` in your browser.
